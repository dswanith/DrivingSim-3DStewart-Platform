# DrivingSim 6-DOF Stewart Platform — Python Dependencies
# Install with: pip3 install -r requirements.txt

# Core numeric stack
numpy>=1.22.0

# Signal processing (for HPF Butterworth filters in MCA)
scipy>=1.8.0

# YAML config loading
pyyaml>=6.0

# WebSocket server
websockets>=10.0

# Optional: CARLA simulator client (for live vehicle data)
# carla>=0.9.14
