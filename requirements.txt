# Stewart-Platform 6-DOF SIL Simulator
# Core dependencies (Python 3.8–3.12)
numpy>=1.20,<2.0
pyyaml>=5.4,<7.0
websockets>=10.0,<13.0

# Development / testing
pytest>=7.0,<9.0

# CARLA integration (optional — only needed for --mode carla)
# Install the wheel matching your CARLA server version:
#   pip install carla==0.9.15
# Do NOT install this unless you have a matching CARLA server.
